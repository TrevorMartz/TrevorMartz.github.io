import styled from 'styled-components';

export const Homepage = () => {
    return <StyledHomepage>Welcome to my app. I am not a designer, just a coder.</StyledHomepage>;
};

const StyledHomepage = styled.div``;
